name: device_worker
version: 0.2.0
description: "Compact remote worker tools for nodes, workspaces, terminals, and macOS GUI control."
author: local
kind: standalone
platforms:
  - linux
  - macos
provides_tools:
  - device_node
  - device_desktop
  - device_workspace
  - device_terminal
